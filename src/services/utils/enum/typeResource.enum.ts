export enum TypeResource {
    media = 'media', //link de youtube o vimeo
    link = 'link', //Jwplayer
    image = 'image',
    live = 'live', //url de trasmision
    audio = 'audio', //url de trasmision
}

export enum TypeResourceDoc {
    jpeg = 'image/jpeg',
    pdf = 'application/pdf',
}

export enum TypeClassroom {
    session = 'session',
    module = 'module',
    event = 'event',
    courseAnvanced = 'course-advanced',
  }